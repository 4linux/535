---
- name: Instala servidor NFS no Debian/Ubuntu
  when: ansible_os_family == "Debian"
  ansible.builtin.apt:
    name: nfs-kernel-server
    state: present
    update_cache: true
  notify:
    - Inicia o NFS no Ubuntu
  tags: nfs

- name: Instala servidor NFS no RedHat/Rocky Linux
  when: ansible_os_family == "RedHat"
  ansible.builtin.yum:
    name: nfs-utils
    state: present
  notify:
    - Habilita o NFS através do firewall do Rocky
    - Inicia o NFS no Rocky
  tags: nfs

- name: Inicia e ativa na inicializacao o servico NFS
  when: ansible_os_family == "RedHat"
  ansible.builtin.systemd:
    name: nfs-server
    state: started
    enabled: true
  tags: nfs
